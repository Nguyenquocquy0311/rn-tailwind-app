import React from 'react';
import { View, Text } from 'react-native';

const HomeScreen = () => {
  return (
    <View className='flex-1 justify-center items-center bg-brand-500'>
      <Text>Home</Text>
    </View>
  );
};

export default HomeScreen;
